--- Animations ---

-- Imports


hl.config({
    animations = {
        enabled = true,
        workspace_wraparound = false,
    },
})

-- hl.animation({
--     leaf = "global",
--     enabled = true,
--     speed = 1,
--     curve = "bezier",
-- })
