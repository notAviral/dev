--- Colors ---

local colors = require "modules.vars.colors"

hl.config({
    general = {
        col = {
            -- TODO : Add dynamic colors support
            -- inactive_border = 
            -- active_border =
            -- nogroup_border =
        },
    },
})
