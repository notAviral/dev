hl.config({
    input = {
        -- Keyboards
        kb_model = "",
        kb_layout = "us",
        kb_variant = "",
        kb_options = "ctrl:nocaps",
        kb_rules = "",

        numlock_by_default = true,

        repeat_rate = 10, --per second
        repeat_delay = 600, --ms
    },
})
